# Dev/test-only dependencies (NOT required to run the API in production).
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.2
httpx2>=2.9          # required by starlette>=1.3 TestClient (fork of httpx)
